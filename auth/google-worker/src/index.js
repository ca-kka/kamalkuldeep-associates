const enc=new TextEncoder();const dec=new TextDecoder();
function b64u(input){const bytes=typeof input==='string'?enc.encode(input):input;let s='';for(const b of bytes)s+=String.fromCharCode(b);return btoa(s).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/g,'')}
function b64(input){const bytes=typeof input==='string'?enc.encode(input):input;let s='';for(const b of bytes)s+=String.fromCharCode(b);return btoa(s)}
function unb64u(s){s=s.replace(/-/g,'+').replace(/_/g,'/');while(s.length%4)s+='=';const bin=atob(s),out=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)out[i]=bin.charCodeAt(i);return out}
function unb64(s){return dec.decode(Uint8Array.from(atob(s.replace(/\n/g,'')),c=>c.charCodeAt(0)))}
async function key(secret){return crypto.subtle.importKey('raw',enc.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign','verify'])}
async function sign(value,secret){const sig=await crypto.subtle.sign('HMAC',await key(secret),enc.encode(value));return `${value}.${b64u(new Uint8Array(sig))}`}
async function verify(value,secret){if(!value)return null;const p=value.lastIndexOf('.');if(p<1)return null;try{return await crypto.subtle.verify('HMAC',await key(secret),unb64u(value.slice(p+1)),enc.encode(value.slice(0,p)))?value.slice(0,p):null}catch{return null}}
function cookie(name,value,maxAge){return `${name}=${value}; Path=/; Max-Age=${maxAge}; HttpOnly; Secure; SameSite=Lax`}
function redirect(url,headers={}){return new Response(null,{status:302,headers:{Location:url,...headers}})}
function json(data,status=200,headers={}){return new Response(JSON.stringify(data),{status,headers:{'Content-Type':'application/json; charset=utf-8',...headers}})}
function b64json(obj){return b64u(JSON.stringify(obj))}
function getCookie(request,name){const cookies=request.headers.get('Cookie')||'';const match=cookies.match(new RegExp('(?:^|; )'+name.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')+'=([^;]+)'));return match?match[1]:''}
async function authenticatedSession(request,secret,allowedEmail){const token=getCookie(request,'kka_session'),raw=await verify(token,secret);if(!raw)return null;try{const payload=JSON.parse(dec.decode(unb64u(raw)));if(!payload.exp||Date.now()>payload.exp||payload.email!==allowedEmail)return null;return payload}catch{return null}}
function validDate(v){return typeof v==='string'&&/^\d{4}-\d{2}-\d{2}$/.test(v)}
function validSlug(v){return typeof v==='string'&&/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(v)&&v.length<=120}
const required=['id','title','slug','category','author','summary'];const publishers=['CA Kamal Jain','CA Hitesh Jain'];
export default{async fetch(request,env){
const url=new URL(request.url),origin=env.PUBLIC_ORIGIN||'https://ca-kka.com',adminPath='/knowledge/admin/',allowedEmail=(env.ADMIN_EMAIL||'').trim().toLowerCase(),secret=env.SESSION_SECRET||'',githubToken=env.GITHUB_PUBLISH_TOKEN||'',repo='ca-kka/kamalkuldeep-associates',dataPath='knowledge/data/articles.json',requestOrigin=request.headers.get('Origin')||'',corsOrigin=['https://ca-kka.com','https://www.ca-kka.com'].includes(requestOrigin)?requestOrigin:'https://ca-kka.com',cors={'Access-Control-Allow-Origin':corsOrigin,'Access-Control-Allow-Credentials':'true','Access-Control-Allow-Methods':'GET, POST, OPTIONS','Access-Control-Allow-Headers':'Content-Type','Vary':'Origin'};
if(request.method==='OPTIONS')return new Response(null,{status:204,headers:cors});if(!env.GOOGLE_CLIENT_ID||!env.GOOGLE_CLIENT_SECRET||!env.REDIRECT_URI||!allowedEmail||!secret)return json({error:'Authentication service is not configured.'},503,cors);if(url.pathname==='/config')return json({clientId:env.GOOGLE_CLIENT_ID,redirectUri:env.REDIRECT_URI,adminPath},200,{...cors,'Cache-Control':'no-store'});
if(url.pathname==='/login'){const state=await sign(b64json({n:crypto.randomUUID(),t:Date.now()}),secret),g=new URL('https://accounts.google.com/o/oauth2/v2/auth');g.searchParams.set('client_id',env.GOOGLE_CLIENT_ID);g.searchParams.set('redirect_uri',env.REDIRECT_URI);g.searchParams.set('response_type','code');g.searchParams.set('scope','openid email profile');g.searchParams.set('state',state);g.searchParams.set('prompt','select_account');return redirect(g.toString(),{'Set-Cookie':cookie('kka_oauth_state',state,600)})}
if(url.pathname==='/callback'){const returned=url.searchParams.get('state')||'',expected=getCookie(request,'kka_oauth_state'),verified=await verify(returned,secret);if(!returned||returned!==expected||!verified)return json({error:'Invalid OAuth state.'},400);const code=url.searchParams.get('code');if(!code)return json({error:'Google did not return an authorization code.'},400);const tr=await fetch('https://oauth2.googleapis.com/token',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({code,client_id:env.GOOGLE_CLIENT_ID,client_secret:env.GOOGLE_CLIENT_SECRET,redirect_uri:env.REDIRECT_URI,grant_type:'authorization_code'})});if(!tr.ok)return json({error:'Google token exchange failed.'},502);const tokens=await tr.json();if(!tokens.access_token)return json({error:'Google did not provide an access token.'},502);const ur=await fetch('https://openidconnect.googleapis.com/v1/userinfo',{headers:{Authorization:`Bearer ${tokens.access_token}`}});if(!ur.ok)return json({error:'Google profile lookup failed.'},502);const user=await ur.json();if(!user.email||user.email.toLowerCase()!==allowedEmail||user.email_verified!==true)return redirect(`${origin}${adminPath}?error=not-authorized`,{'Set-Cookie':cookie('kka_session','',0)});const sp=b64json({email:user.email.toLowerCase(),name:user.name||'',picture:user.picture||'',exp:Date.now()+8*60*60*1000}),session=await sign(sp,secret);return redirect(`${origin}${adminPath}?login=success`,{'Set-Cookie':[cookie('kka_session',session,8*60*60),cookie('kka_oauth_state','',0)].join(', ')})}
const session=await authenticatedSession(request,secret,allowedEmail);if(url.pathname==='/session'){if(!session)return json({authenticated:false},401,{...cors,'Cache-Control':'no-store'});return json({authenticated:true,user:{email:session.email,name:session.name,picture:session.picture}},200,{...cors,'Cache-Control':'no-store'})}
if(url.pathname==='/articles'&&request.method==='GET'){if(!session)return json({error:'Authentication required.'},401,cors);if(!githubToken)return json({error:'Publishing service is not configured.'},503,cors);const gh={'Accept':'application/vnd.github+json','Authorization':`Bearer ${githubToken}`,'X-GitHub-Api-Version':'2026-03-10','User-Agent':'KKA-Knowledge-Centre-Publisher'},api=`https://api.github.com/repos/${repo}/contents/${dataPath}`,r=await fetch(`${api}?ref=main`,{headers:gh});if(!r.ok)return json({error:'Could not read Knowledge Centre data from GitHub.',githubStatus:r.status},502,cors);const cur=await r.json();try{const data=JSON.parse(unb64(cur.content||''));return json({items:Array.isArray(data.items)?data.items:[]},200,{...cors,'Cache-Control':'no-store'})}catch{return json({error:'Existing Knowledge Centre data is not valid JSON.'},502,cors)}}
if(url.pathname==='/upload-image'&&request.method==='POST'){if(!session)return json({error:'Authentication required.'},401,cors);if(!githubToken)return json({error:'Publishing service is not configured.'},503,cors);let p;try{p=await request.json()}catch{return json({error:'Invalid JSON payload.'},400,cors)}const mime=String(p.mime||''),dataUrl=String(p.dataUrl||''),name=String(p.filename||'image').replace(/[^a-zA-Z0-9._-]/g,'-');if(!['image/jpeg','image/png','image/webp','image/gif'].includes(mime)||!dataUrl.startsWith(`data:${mime};base64,`))return json({error:'Unsupported image.'},400,cors);const raw=dataUrl.split(',')[1]||'';if(raw.length>7*1024*1024)return json({error:'Image is too large.'},413,cors);const ext={'image/jpeg':'jpg','image/png':'png','image/webp':'webp','image/gif':'gif'}[mime],safeBase=name.replace(/\.[^.]+$/,'').slice(0,80)||'image',path=`knowledge/images/articles/${Date.now()}-${crypto.randomUUID().slice(0,8)}-${safeBase}.${ext}`,api=`https://api.github.com/repos/${repo}/contents/${path}`,gh={'Accept':'application/vnd.github+json','Authorization':`Bearer ${githubToken}`,'X-GitHub-Api-Version':'2026-03-10','User-Agent':'KKA-Knowledge-Centre-Publisher'};const r=await fetch(api,{method:'PUT',headers:{...gh,'Content-Type':'application/json'},body:JSON.stringify({message:'Add Knowledge Centre article image',content:raw,branch:'main'})});if(!r.ok)return json({error:'GitHub image upload failed.',githubStatus:r.status},502,cors);return json({success:true,url:`https://ca-kka.com/${path}`},200,{...cors,'Cache-Control':'no-store'})}
if((url.pathname==='/publish'||url.pathname==='/manage')&&request.method==='POST'){if(!session)return json({error:'Authentication required.'},401,cors);if(!githubToken)return json({error:'Publishing service is not configured.'},503,cors);let p;try{p=await request.json()}catch{return json({error:'Invalid JSON payload.'},400,cors)}const gh={'Accept':'application/vnd.github+json','Authorization':`Bearer ${githubToken}`,'X-GitHub-Api-Version':'2026-03-10','User-Agent':'KKA-Knowledge-Centre-Publisher'},api=`https://api.github.com/repos/${repo}/contents/${dataPath}`,r=await fetch(`${api}?ref=main`,{headers:gh});if(!r.ok)return json({error:'Could not read Knowledge Centre data from GitHub.',githubStatus:r.status},502,cors);const current=await r.json();let data;try{data=JSON.parse(unb64(current.content||''))}catch{return json({error:'Existing Knowledge Centre data is not valid JSON.'},502,cors)}if(!Array.isArray(data.items))data.items=[];let message='';if(url.pathname==='/manage'){const action=p.action,id=typeof p.id==='string'?p.id.trim():'';if(!id)return json({error:'Article ID is required.'},400,cors);const i=data.items.findIndex(x=>x.id===id);if(i<0)return json({error:'Article not found.'},404,cors);if(action==='unpublish'){data.items[i]={...data.items[i],status:'draft',updatedAt:new Date().toISOString().slice(0,10)};message='Article unpublished and kept as a draft.'}else if(action==='delete'){data.items.splice(i,1);message='Article permanently deleted.'}else return json({error:'Unsupported management action.'},400,cors)}else{if(required.some(k=>typeof p[k]!=='string'||!p[k].trim()))return json({error:'Article is missing a required field.'},400,cors);if(!publishers.includes(p.author))return json({error:'Select an approved KKA publisher.'},400,cors);if(!validSlug(p.slug))return json({error:'Slug must contain only lowercase letters, numbers and hyphens.'},400,cors);if(!validDate(p.publishedAt)||!validDate(p.updatedAt))return json({error:'Published and updated dates must use YYYY-MM-DD.'},400,cors);if(!['approved','published'].includes(p.status))return json({error:'Only Approved or Published articles can be published.'},400,cors);if(typeof p.contentHtml!=='string'||!p.contentHtml.trim())return json({error:'Article content is required.'},400,cors);const normalized={id:p.id.trim(),status:'published',title:p.title.trim(),slug:p.slug.trim(),category:p.category.trim(),author:p.author,publishedAt:p.publishedAt,updatedAt:p.updatedAt,summary:p.summary.trim(),tags:Array.isArray(p.tags)?p.tags.map(x=>String(x).trim()).filter(Boolean).slice(0,30):[],contentHtml:p.contentHtml.trim()};if(data.items.some(x=>x.slug===normalized.slug&&x.id!==normalized.id))return json({error:'Another article already uses this slug.'},409,cors);const i=data.items.findIndex(x=>x.id===normalized.id);if(i>=0)data.items[i]={...data.items[i],...normalized};else data.items.push(normalized);message=i>=0?'Article updated and published.':'Article published.'}const ur=await fetch(api,{method:'PUT',headers:{...gh,'Content-Type':'application/json'},body:JSON.stringify({message:`${message} KKA Knowledge Centre`,content:b64(JSON.stringify({...data,version:1},null,2)+'\n'),sha:current.sha,branch:'main'})});const text=await ur.text();if(!ur.ok)return json({error:ur.status===409?'GitHub data changed while saving. Refresh and try again.':'GitHub update failed.',githubStatus:ur.status},502,cors);let result={};try{result=JSON.parse(text)}catch{}return json({success:true,message,commit:result.commit?.sha||null},200,{...cors,'Cache-Control':'no-store'})}
if(url.pathname==='/logout')return redirect(`${origin}${adminPath}`,{'Set-Cookie':cookie('kka_session','',0)});return json({service:'KKA Knowledge Centre Google Authentication',status:'ok'},200,cors)}};