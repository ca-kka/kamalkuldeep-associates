const categories=[['GST','Goods & Services Tax'],['Income Tax','Direct tax updates'],['TDS / TCS','Withholding tax'],['MCA','Companies & LLP compliance'],['Audit & Accounting','Standards & practice'],['FEMA / RBI','Foreign exchange & banking'],['SEBI','Capital markets & regulation'],['Case Laws','Judgments & rulings'],['Compliance','Due dates & action points'],['Practical Guides','Step-by-step resources'],['FAQs','Common professional questions']];
const catEl=document.getElementById('categories'),artEl=document.getElementById('articles'),search=document.getElementById('search'),count=document.getElementById('count');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function renderCats(){catEl.innerHTML=categories.map(c=>`<a class="category" href="categories.html?q=${encodeURIComponent(c[0])}"><span>${esc(c[0])}</span><small>${esc(c[1])}</small></a>`).join('')}
function renderArticles(rows,q=''){const s=q.trim().toLowerCase(),filtered=rows.filter(a=>a.status==='published'&&(!s||[a.title,a.category,a.summary,a.author,(a.tags||[]).join(' ')].join(' ').toLowerCase().includes(s)));count.textContent=`${filtered.length} item${filtered.length===1?'':'s'}`;artEl.innerHTML=filtered.length?filtered.map(a=>`<article class="article"><div class="article-body"><span class="tag">${esc(a.category)}</span><h3><a href="article.html?slug=${encodeURIComponent(a.slug)}" style="color:inherit;text-decoration:none">${esc(a.title)}</a></h3><p>${esc(a.summary)}</p><div class="meta">${esc(a.author)} · ${new Date(a.publishedAt+'T00:00:00').toLocaleDateString('en-IN',{day:'numeric',month:'short',year:'numeric'})}</div></div></article>`).join(''):'<p class="muted">No matching knowledge items found.</p>'}
renderCats();fetch('data/articles.json').then(r=>r.json()).then(d=>{const rows=d.items||[];renderArticles(rows);search.addEventListener('input',e=>renderArticles(rows,e.target.value));const q=new URLSearchParams(location.search).get('q');if(q){search.value=q;renderArticles(rows,q)}}).catch(()=>{artEl.innerHTML='<p class="muted">Knowledge data is temporarily unavailable.</p>'});
