document.addEventListener('DOMContentLoaded',()=>{const button=document.getElementById('publish');if(!button)return;button.addEventListener('click',()=>{let tries=0;const timer=setInterval(async()=>{tries++;const status=document.getElementById('save-status'),text=(status?.textContent||'').toLowerCase();if(text.includes('published successfully')){clearInterval(timer);const title=document.getElementById('article-title')?.value||'Article';const filter=document.getElementById('article-filter');if(filter)filter.value='published';if(typeof loadArticles==='function')await loadArticles();if(typeof newArticle==='function')newArticle();if(filter)filter.value='published';if(typeof loadArticles==='function')await loadArticles();if(status)status.textContent=`“${title}” is published. The editor is ready for a new article.`;document.querySelector('.article-manager')?.scrollIntoView({behavior:'smooth',block:'start'});}else if(tries>=60){clearInterval(timer);}},250);});});
