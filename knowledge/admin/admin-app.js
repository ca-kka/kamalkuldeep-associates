const KEY='kka-knowledge-admin-draft-v1';
const $=s=>document.querySelector(s);
function saveDraft(){const data={id:$('#article-id')?.value||'',status:$('#article-status')?.value||'draft',title:$('#article-title')?.value||'',slug:$('#article-slug')?.value||'',category:$('#article-category')?.value||'GST',author:$('#article-author')?.value||'KKA',publishedAt:$('#article-published')?.value||'',updatedAt:$('#article-updated')?.value||'',summary:$('#article-summary')?.value||'',tags:($('#article-tags')?.value||'').split(',').map(x=>x.trim()).filter(Boolean),content:$('#article-content')?.value||''};localStorage.setItem(KEY,JSON.stringify(data));return data}
function loadDraft(){try{const d=JSON.parse(localStorage.getItem(KEY)||'null');if(!d)return;Object.entries(d).forEach(([k,v])=>{const el=$('#article-'+k);if(el)el.value=Array.isArray(v)?v.join(', '):v})}catch(e){}}
function preview(){const d=saveDraft();const html=`<span class="tag">${escapeHtml(d.category)}</span><h1>${escapeHtml(d.title||'Untitled article')}</h1><p class="preview-meta">${escapeHtml(d.author)} · ${escapeHtml(d.updatedAt||d.publishedAt||'Draft')}</p><div class="preview-summary"><strong>Executive Summary</strong><p>${escapeHtml(d.summary)}</p></div><div class="preview-content">${escapeHtml(d.content).replace(/\n/g,'<br>')}</div>`;const w=window.open('about:blank','_blank');if(w){w.document.write('<!doctype html><title>KKA Preview</title><style>body{font-family:Segoe UI,Arial;max-width:850px;margin:50px auto;padding:20px;color:#172033;line-height:1.7}.tag{background:#edf3ff;color:#2a5298;padding:5px 10px;border-radius:20px;font-weight:700}.preview-summary{background:#f7f9fc;padding:18px;border-left:4px solid #1e3c72;margin:24px 0}.preview-meta{color:#68758a}</style>'+html);w.document.close()}}
function exportDraft(){const d=saveDraft();const blob=new Blob([JSON.stringify({version:1,items:[{...d}]},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=(d.slug||'kka-article')+'.json';a.click();URL.revokeObjectURL(a.href)}
function clearDraft(){localStorage.removeItem(KEY);location.reload()}
function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
document.addEventListener('DOMContentLoaded',()=>{loadDraft();$('#save-draft')?.addEventListener('click',()=>{saveDraft();$('#save-status').textContent='Draft saved on this device.'});$('#preview')?.addEventListener('click',preview);$('#export')?.addEventListener('click',exportDraft);$('#clear-draft')?.addEventListener('click',clearDraft);$('#article-title')?.addEventListener('input',()=>{if(!$('#article-slug').value)$('#article-slug').value=$('#article-title').value.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')})});