# Admin UI correction

- Replaced GitHub-navigation dashboard with an interactive KKA publishing workspace.
- Added local draft save, preview and JSON export.
- Added an admin ID/password login surface that remains fail-closed until server-side authentication is connected.
- No credentials or GitHub tokens are embedded in the public site.
