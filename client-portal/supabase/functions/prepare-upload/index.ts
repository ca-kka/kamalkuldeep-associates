import { cleanFilename, corsHeaders, json, requireStaff } from "../_shared/portal.ts";

const months: Record<string, number> = { jan:1,january:1,feb:2,february:2,mar:3,march:3,apr:4,april:4,may:5,jun:6,june:6,jul:7,july:7,aug:8,august:8,sep:9,september:9,oct:10,october:10,nov:11,november:11,dec:12,december:12 };
const compact=(v:string)=>v.toUpperCase().replace(/[^A-Z0-9]/g,"");
const normal=(v:string)=>v.toUpperCase().replace(/[^A-Z0-9]+/g," ").trim();
const areas=new Set(["gst","tds","income_tax","accounts","mca","other"]);
function classify(filename:string,clients:any[]){const raw=filename.toUpperCase(),flat=compact(filename),words=normal(filename);const exact=clients.find(c=>[c.pan,c.tan,c.cin,c.gstin].filter(Boolean).some((id:string)=>flat.includes(compact(id))));let client=exact,confidence=exact?100:0,reasons:string[]=exact?["Exact PAN/TAN/CIN/GSTIN found in filename"]:[];if(!client){client=clients.find(c=>[c.legal_name,...(c.filename_aliases??[])].filter(Boolean).some((name:string)=>{const n=normal(name);return n.length>=5&&words.includes(n)}));if(client){confidence=84;reasons.push("Client legal name or alias found in filename")}}let area="other";if(/GST|GSTR|GSTR1|GSTR3B/.test(raw))area="gst";else if(/TDS|24Q|26Q|27Q|27EQ/.test(raw))area="tds";else if(/\bITR\b|INCOME[ _-]?TAX|FORM[ _-]?16/.test(raw))area="income_tax";else if(/BALANCE[ _-]?SHEET|TRIAL[ _-]?BALANCE|LEDGER|FINANCIALS?|P&L/.test(raw))area="accounts";if(area!=="other")reasons.push(`Document area inferred as ${area}`);const fy=raw.match(/(?:FY|F\.Y\.?)[ _-]?(20\d{2})[ _-]?(?:-|TO)?[ _-]?(\d{2}|20\d{2})/)??raw.match(/\b(20\d{2})-(\d{2})\b/);let financialYear:string|null=null;if(fy){financialYear=`${fy[1]}-${fy[2].length===2?fy[2]:fy[2].slice(2)}`;reasons.push(`Financial year inferred as ${financialYear}`)}const month=Object.keys(months).sort((a,b)=>b.length-a.length).find(n=>new RegExp(`\\b${n}\\b`,"i").test(filename));const q=raw.match(/(?:\bQ([1-4])\b|QUARTER[ _-]?([1-4]))/);const period=area==="gst"&&month?`${months[month]}`.padStart(2,"0"):area==="tds"&&q?`Q${q[1]??q[2]}`:null;if(period)reasons.push(`Period inferred as ${period}`);return{clientId:client?.id??null,confidence,reasons,area,financialYear,period}}
async function node(service:any,id:string){const {data,error}=await service.from("filing_nodes").select("id,parent_id,name,node_type,enabled,client_upload_enabled,requires_financial_year,period_mode").eq("id",id).maybeSingle();if(error)throw error;if(!data||!data.enabled)throw new Error("Selected filing folder is unavailable.");return data}
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders});try{const{user,service}=await requireStaff(req);const body=await req.json(),filename=cleanFilename(String(body.filename??"")),sha256=String(body.sha256??"").toLowerCase(),byteSize=Number(body.byteSize),contentType=String(body.contentType??"application/octet-stream");if(!filename||!/^[a-f0-9]{64}$/.test(sha256)||!Number.isSafeInteger(byteSize)||byteSize<1||byteSize>52428800)return json({error:"Invalid file metadata"},400);const{data:clients,error:clientError}=await service.from("clients").select("id, legal_name, pan, tan, cin, gstin, filename_aliases").eq("active",true);if(clientError)throw clientError;let result=classify(filename,clients??[]),filingNodeId:string|null=null,filingNode:any=null;if(body.manual===true){const clientId=String(body.clientId??""),suppliedNodeId=String(body.filingNodeId??""),area=String(body.area??""),financialYear=body.financialYear?String(body.financialYear):null,period=body.period?String(body.period):null;if(!clientId)return json({error:"A client is required"},400);const selected=(clients??[]).find(c=>c.id===clientId);if(!selected)return json({error:"Selected client is not active or could not be found"},400);if(suppliedNodeId){filingNode=await node(service,suppliedNodeId);if(!filingNode.client_upload_enabled)return json({error:"This filing folder is not enabled for uploads"},400);filingNodeId=filingNode.id;result={clientId,confidence:100,reasons:["Manual classification selected by KKA staff",`Filing folder selected: ${filingNode.name}`],area:areas.has(area)?area:"other",financialYear,period}}else{if(!areas.has(area))return json({error:"Invalid manual classification"},400);if(area==="mca"&&!selected.cin)return json({error:"MCA is available only for client profiles with a CIN"},400);result={clientId,confidence:100,reasons:["Manual classification selected by KKA staff"],area,financialYear,period}}}if(result.clientId){const{data:duplicate,error:duplicateError}=await service.from("documents").select("id,client_id").eq("client_id",result.clientId).eq("sha256",sha256).is("deleted_at",null).limit(1).maybeSingle();if(duplicateError)throw duplicateError;if(duplicate)return json({state:"duplicate",existingDocumentId:duplicate.id,message:"An identical file is already stored for this client; no upload URL was issued."},409)}const objectPath=`staging/${crypto.randomUUID()}`,expiresAt=new Date(Date.now()+8*60*1000).toISOString();const{data:upload,error:uploadError}=await service.from("document_uploads").insert({requested_by:user.id,original_filename:String(body.filename),sanitized_filename:filename,content_type:contentType,byte_size:byteSize,sha256,object_path:objectPath,proposed_client_id:result.clientId,proposed_area:result.area,proposed_financial_year:result.financialYear,proposed_period:result.period,proposed_filing_node_id:filingNodeId,confidence:result.confidence,reasons:result.reasons,expires_at:expiresAt}).select().single();if(uploadError)throw uploadError;const{data:signed,error:signedError}=await service.storage.from("client-documents").createSignedUploadUrl(objectPath);if(signedError)throw signedError;return json({state:"prepared",uploadId:upload.id,signedUrl:signed.signedUrl,token:signed.token,expiresAt,classification:{...result,filingNodeId,filingNode:filingNode?{id:filingNode.id,name:filingNode.name,periodMode:filingNode.period_mode,requiresFinancialYear:filingNode.requires_financial_year}:null}})}catch(error){return json({error:error instanceof Error?error.message:"Upload could not be prepared"},400)}});
