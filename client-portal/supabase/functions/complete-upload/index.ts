import { corsHeaders, json, requireStaff, sha256Hex } from "../_shared/portal.ts";
const SUPABASE_URL=Deno.env.get("SUPABASE_URL")!;
Deno.serve(async req=>{if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders});try{const{user,service}=await requireStaff(req),{uploadId}=await req.json();if(!uploadId)return json({error:"uploadId is required"},400);const{data:upload,error:ue}=await service.from("document_uploads").select("*").eq("id",uploadId).eq("requested_by",user.id).maybeSingle();if(ue)throw ue;if(!upload)return json({error:"Upload request not found"},404);const{data:existing,error:ee}=await service.from("documents").select("id,status,client_id,original_filename,area,financial_year,filing_period,classification_confidence,filing_node_id").eq("upload_id",upload.id).maybeSingle();if(ee)throw ee;if(existing){if(!upload.completed_at)await service.from("document_uploads").update({completed_at:new Date().toISOString()}).eq("id",upload.id);return json({state:existing.status,documentId:existing.id,classification:{clientId:existing.client_id,area:existing.area,financialYear:existing.financial_year,period:existing.filing_period,confidence:existing.classification_confidence,filingNodeId:existing.filing_node_id},oneDrive:null,oneDriveRegistryLinked:true,requiresReview:existing.status==="review",alreadyFinalized:true})}if(upload.completed_at)return json({error:"Upload was already completed but its document record could not be found."},409);if(new Date(upload.expires_at)<new Date())return json({error:"Upload URL expired; request a new one"},410);if(upload.proposed_client_id&&upload.sha256){const{data:dup,error:de}=await service.from("documents").select("id,status,client_id,original_filename,area,financial_year,filing_period,classification_confidence,filing_node_id").eq("client_id",upload.proposed_client_id).eq("sha256",upload.sha256).is("deleted_at",null).limit(1).maybeSingle();if(de)throw de;if(dup){await service.from("document_uploads").update({completed_at:new Date().toISOString()}).eq("id",upload.id);return json({state:dup.status,documentId:dup.id,classification:{clientId:dup.client_id,area:dup.area,financialYear:dup.financial_year,period:dup.filing_period,confidence:dup.classification_confidence,filingNodeId:dup.filing_node_id},oneDrive:null,oneDriveRegistryLinked:true,requiresReview:dup.status==="review",duplicate:true,alreadyFinalized:true})}}const{data:object,error:de}=await service.storage.from("client-documents").download(upload.object_path);if(de||!object)throw new Error("Uploaded object is unavailable");const bytes=await object.arrayBuffer();if(bytes.byteLength!==upload.byte_size||await sha256Hex(bytes)!==upload.sha256)throw new Error("Uploaded file checksum does not match the requested file");const reasons=Array.isArray(upload.reasons)?upload.reasons:[],manual=reasons.includes("Manual classification selected by KKA staff");const fy=upload.proposed_financial_year?`/${upload.proposed_financial_year}`:"",period=upload.proposed_period?`/${upload.proposed_period}`:"";const finalPath=upload.proposed_client_id?`${upload.proposed_client_id}/${upload.proposed_filing_node_id??upload.proposed_area}${fy}${period}/${crypto.randomUUID()}`:`review/${crypto.randomUUID()}`;const{error:moveError}=await service.storage.from("client-documents").move(upload.object_path,finalPath);if(moveError)throw moveError;let oneDrive:any=null;if(upload.proposed_client_id&&upload.proposed_filing_node_id){const auth=req.headers.get("Authorization");if(!auth)throw new Error("Missing staff session");const r=await fetch(`${SUPABASE_URL}/functions/v1/filing-storage`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:auth},body:JSON.stringify({operation:"upload_from_supabase",storagePath:finalPath,filename:upload.original_filename,clientId:upload.proposed_client_id,filingNodeId:upload.proposed_filing_node_id,area:upload.proposed_area??"other",financialYear:upload.proposed_financial_year??null,filingPeriod:upload.proposed_period??null})}),b=await r.json().catch(()=>({}));if(!r.ok||!b.success)throw new Error(b.error||`OneDrive transfer failed (HTTP ${r.status})`);oneDrive={driveItemId:b.item?.id??null,webUrl:b.item?.webUrl??null,parentDriveItemId:b.parentDriveItemId??null,registryId:b.registry?.id??null}}else if(manual&&upload.proposed_client_id&&upload.proposed_area){const auth=req.headers.get("Authorization");if(!auth)throw new Error("Missing staff session");const r=await fetch(`${SUPABASE_URL}/functions/v1/onedrive-storage`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:auth},body:JSON.stringify({operation:"upload_from_supabase",storagePath:finalPath,filename:upload.original_filename,clientId:upload.proposed_client_id,area:upload.proposed_area,financialYear:upload.proposed_financial_year??null,filingPeriod:upload.proposed_period??null})}),b=await r.json().catch(()=>({}));if(!r.ok||!b.success)throw new Error(b.error||`OneDrive transfer failed (HTTP ${r.status})`);oneDrive={driveItemId:b.item?.id??null,webUrl:b.item?.webUrl??null,parentDriveItemId:b.destination?.parentDriveItemId??null,registryId:b.registry?.id??null}}const status=manual||upload.confidence===100&&upload.proposed_area!=="other"&&upload.proposed_financial_year?"accepted":"review";const inserted=await service.from("documents").insert({client_id:upload.proposed_client_id,upload_id:upload.id,original_filename:upload.original_filename,storage_path:finalPath,content_type:upload.content_type,byte_size:upload.byte_size,sha256:upload.sha256,area:upload.proposed_area,financial_year:upload.proposed_financial_year,filing_period:upload.proposed_period,filing_node_id:upload.proposed_filing_node_id,status,classification_confidence:upload.confidence,classification_reasons:upload.reasons,uploaded_by:user.id}).select().single();let document:any=inserted.data;if(inserted.error){const retry=await service.from("documents").select("id,status,client_id,original_filename,area,financial_year,filing_period,classification_confidence,filing_node_id").eq("upload_id",upload.id).maybeSingle();if(retry.error)throw inserted.error;if(!retry.data)throw new Error(inserted.error.message||"Document record could not be finalized");document=retry.data}let registryLinkError:string|null=null;if(oneDrive?.registryId){const{error:e}=await service.from("onedrive_file_registry").update({document_id:document.id,deleted_at:null,deletion_source:null,classification_status:"classified",updated_at:new Date().toISOString()}).eq("id",oneDrive.registryId);if(e)registryLinkError=e.message||"OneDrive registry link could not be updated"}const{error:ce}=await service.from("document_uploads").update({completed_at:new Date().toISOString()}).eq("id",upload.id);if(ce)throw ce;await service.from("audit_logs").insert({actor_id:user.id,client_id:document.client_id,action:"document_uploaded",entity_type:"document",entity_id:document.id,metadata:{filename:upload.original_filename,state:document.status,confidence:upload.confidence,manual,filingNodeId:upload.proposed_filing_node_id??null,oneDrive:oneDrive??null,oneDriveRegistryLinked:!registryLinkError,oneDriveRegistryLinkError:registryLinkError}});return json({state:document.status,documentId:document.id,classification:{clientId:document.client_id,area:document.area,financialYear:document.financial_year,period:document.filing_period,confidence:document.classification_confidence,filingNodeId:document.filing_node_id},oneDrive,oneDriveRegistryLinked:!registryLinkError,oneDriveRegistryLinkError:registryLinkError,requiresReview:document.status==="review"})}catch(error){return json({error:error instanceof Error?error.message:"Upload could not be completed"},400)}});
